{
    "$schema": "https://raw.githubusercontent.com/FredHutch/bash-workbench/main/docs/schema.json",
    "name": "OCTAPUS - fetchFTP",
    "description": "Download a collection of genomes from NCBI",
    "args": {
        "genomes": {
            "help": "CSV file listing genomes to download (from https://www.ncbi.nlm.nih.gov/genome/browse)",
            "wb_type": "file",
            "required": true
        },
        "nxf_profile": {
            "help": "Profile used for resource allocation (standard or testing)",
            "wb_env": "PROFILE",
            "wb_type": "select",
            "wb_choices": ["standard", "testing"],
            "default": "standard"
        }
    }
}

